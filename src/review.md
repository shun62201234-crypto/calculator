よく使うTSDocタグ一覧
@param 引数の説明
@returns 戻り値の説明
@remarks 補足・注意事項（長めOK）
@throws 例外（エラー）の説明
@example 使用例

// Rev3 2026.4.24
// namespeceを使用して
結果が同じif文を２つ使用しているので１つのif文に入れ込む。
if (this.isEmpty() || this.inputValue === Config.Input.DEFAULT_DISPLAY_VALUE) {
            this.inputValue = digit.toString();
            return;
        }

KeyTokenの定数化→KEY_KINDを作成

Configのnamespacce使用→型アサーションでもnamespece同様に種別分けできたのでそちらで代用



// Rev2 2026.4.10
使い方が理解できてるか。呼び出しているところのコードがかけてるのか？
関数ヘッダは書ききることinputtbefferがどういう風に使われてほしいのかをかききらないといけない研修資料ではどういう責務をもっているのか入力されたのを保持しておく。入力された値を管理しとく意味。桁数上限を入力判定を超えたら受け付けないように保持管理している。小数点記号がおされたらこいつがよびだされる。カリキュレーターが状態遷移をもっている。keymapper、left,operator,inputtobefferが呼び出されていることがまずピンときてるかどうか。シーケンスを読み取ることlettは何してる？数式における左側の数値になるこれをleftという変数にいれてるだけ今入力されてるt値がbufferになる。記号をおされたら覚えてた値は次の値を覚えないといけないので消去するだからクリアしてる。今入力されている数値を覚えておく。桁数制限もこいつがもっておかないといけない。
メンバー変数はTSDocを記載すること。valueは何のvalueか分からないから入力された数値を意味する文字列など可読性を上げる記述をすること。
先頭の0だった場合0を覚えてるが問題ないのか？それがやりたいの？
今が0なら上書きするからか？いや、this.valueは初期値0なん？空なん？コンストラクタで設定できるようにしているので、どんな数値でも入るようになっている。this.valueは初期値はそもそもコンストラクタでコンストラクタを外から渡せる必要あある？かならず0か空のどちらかなので、MAXDIGITだけでいいのではないのか。
マジックナンバーは定数で定義して。ZEROと定義して低数値で設定して定数用ファイルを用意してつくって。
桁数チェックはほんとに仕様とあってるか確認することvalueには小数点とマイナスがはいることがあるですよね。除いて8桁になっているのか今の状態では除けていない。正規表現ではなく一時的に覗いて8桁にする。ほかの処理でも使うかもしれないので桁数チェックの関数を用意してそれをif文で呼び出した方がいいのかなと思う。
// --- 桁上限（異常・特別処理）--- 
        if (this.value.length >= this.maxDigits) {
            console.log(`既に${this.maxDigit}桁分だけ入力されていますvalue=${this.value}`)
            return;
        }
ここにちゃんとはいったんやなというのがこのクラスでは状態がわかるので、上記の用にログを残す。不要なログはいらないが必要なログはのこす。デバックのときだけならconsole.debugで記載すればデバックのときしかでないようにもできる。今回はログにのこしてもいいと思うのでlogで残す方がいい。ここも一緒ですよね小数点が含まれていることもlogとして出してた方がいい
public pushDecimal(): void {
        // --- 重複防止（異常・特別処理）---
        if (this.value.includes(".")){  // もし（すでに "." が含まれている）
            return;                     // 何もしない(処理終了)
        }
        // --- 先頭0制御（異常・特別処理）---
        if (this.value === "") {        // もし(valueが空) {
            this.value = "0.";          // "0." を作る
            return;                     // 処理終了
        } 
        // --- 小数点をthis.valueに追加 ---
        this.value += ".";
    }
小数点がおされたときに０がはいっていたら０．も初期値で0が入っているかどうか初期値が0または空文字なのかと思うが小城の実装のしかたかな。空なら処理終了ともしかくなら
toNumber()のところも空文字なのか0なのかどっちなのかこれは空文字なら0に扱うからいいのか。。。
isEmpty関数の空文字を用意しているのなら、
public toNumber(): number {
        if (this.value === "") {        // もし(this.valueが空白""なら) 
            return 0;                   // 数字の０にする
        } else {                        // それ以外なら
            return Number(this.value)   // 文字列を数値に変える
        }数字になってなかったらどのようにエラーチェックするのかNaN？
    }
ではなく
public toNumber(): number {
        if (this.isEmpty()) {        // もし(this.valueが空白""なら) 
            return 0;                   // 数字の０にする
        } else {                        // それ以外なら
            return Number(this.value)   // 文字列を数値に変える
        }数字になってなかったらどのようにエラーチェックするのかNaN？
    }
と記載すべきコピーコード見たいになってるので、isEmptyにまたは０も含んで記載していれば、同じ条件なのでisEputyをつかうべき。そとから呼び出すためのpublicかもしれないが内部でもつかいまわしましょう！
digitCountで桁数チェックしているからさっきいってたif文もできるのではないのか。
全体像を理解してから実装すべき。使い方を理解してからコードを記載すべき。
NaNチェックはstring型はいらないしいて言うならthis.valueが数値なってない場合は例外が発生するなかなそれかＮａＮになるのかなってないときのエラーをどこできずくのか確認しとくこと。



// Rev1 2026.4.3 
・enumからconstアサーションで記述する。
・適宜ファイル分けて作成する。
・opはoperatorなど短縮しない意味わかる変数名にする
・関数ヘッダ記載する。例外処理の場合@throws どんな例外をなげるのかどんな時に発生するのか　例：@throws DivisionByZeroError 0で割る操作が行われた時に発生
・メンバー変数の説明を記述する。valueは何のvalueか分からないから
・マジックナンバーは使わず定数化する。
・if文で1行だとしてもブロックはつくる。省略しない。
・コード内にデバック用の関数を作るのではなくテストファイルを作成する。
・this.valueが数字以外が入らないように防いでいるのか？
・7なぜinputbufferからコードを記載始めたのか？実装で一番大事なのは、コードを書いてすぐ動くして問題なく書けてるのかを見ていく。エントリーをつくれないと動きがみにくいからコードは動かしながら実装するのがよい。ちゃんと動くのか担保するために思い通りの動きをしているのか単体試験しながらすすめる。動くことを前提にレビューはする。
・Evaluatorクラスがインスタンスを作る必要があるのか？スタティックにすればインスタンスつくらずにすぐよびだせるのでは？メンバー変数つかってないので。単純に計算するだけなので。スタティックならEvaluator.computeでいけるけどインスタンスつくるならnew Evaluatorしないといけないので手順が増える。

